---
interface Props {
  class?: string;
  as?: 'div' | 'article' | 'section';
}
const { class: className = '', as: Tag = 'div' } = Astro.props;
---

<Tag class={`glass rounded-3xl p-6 md:p-8 ${className}`}>
  <slot />
</Tag>
